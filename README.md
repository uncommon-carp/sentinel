# Sentinel

Sentinel is a CLI-based API security scanner. Point it at a base URL (optionally OpenAPI later) and it runs passive + active checks, outputting JSON and Markdown reports.

## Quickstart

```bash
npm i
npm run build
node dist/cli/index.js scan -u https://example.com

```
